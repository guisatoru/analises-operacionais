# Copie este arquivo para .env e preencha com seus valores reais.
# O .env nunca deve ser commitado (já está no .gitignore).

# Chave secreta do Django (gere uma nova para produção).
SECRET_KEY=troque-isso-por-uma-chave-secreta-real

# Coloque False em produção.
DEBUG=True

# Hosts permitidos, separados por vírgula. Use * apenas em desenvolvimento.
ALLOWED_HOSTS=*

# String de conexão com o PostgreSQL do Supabase.
# Pegue em: Supabase > Project Settings > Database > Connection string > URI
# Exemplo: postgresql://postgres:SUA_SENHA@db.xxxxxxxxxxxxx.supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:SENHA@db.SEU_PROJETO.supabase.co:5432/postgres
